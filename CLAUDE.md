# Claude Code 設定

## Git 規約

### ブランチ命名
- わかりやすく英語で記述する
- 例：`feature/add-notification-system`、`fix/twilio-webhook-error`

### コミットメッセージ
- **言語**: 日本語で記述する
- **Gitmoji・絵文字は使わない**（2026-07 に廃止。過去の履歴に残っている絵文字付きコミットは踏襲しない）
- **スタイルは git history に合わせる**: 書き出す前に `git log --oneline -15` で直近の履歴を確認し、そのリポジトリの既存スタイル（`operator:` のようなスコープ接頭辞の有無・文体・粒度）に揃える
- 1行目は端的に作業内容を要約し、必要に応じて本文で背景・影響範囲・検証内容を補足する

#### コミットメッセージ例
```
operator: PR #7038 レビュー指摘6件を反映する
Renovateを廃止し、パッケージ更新をbulk-package-updateスキルに一本化
control-center: Auth0 テナントドメインを実値に修正する
```

### コミット・PR の依頼時
- コミット単体の依頼時は、本ファイルのGit規約（コミットメッセージ言語・スタイル等）に従って通常のコミットフローで対応する
- PR作成まで依頼された場合: 差分をセルフレビューして見つけた問題を直してからコミット・push する。PRは必ず `gh pr create --draft` で作成し、タイトル・説明は日本語（Why ファースト: 概要→背景→変更点→動作確認。PRサイズに応じて簡潔に）

### 禁止事項
- `git push --force` / `git push --force-with-lease` は絶対に使用しない。rebase で履歴が diverge した場合は、新しいブランチを作って対応する

### プルリクエスト
- **タイトル**: 日本語で記述
- **説明**: 日本語で記述
- **承認**: 1人の approve が必要

## パッケージ追加

新規パッケージの導入前に `npm view <pkg> time --json` で最新版のリリース日を確認し、**リリースから7日未満なら導入せずユーザーに確認する**（サプライチェーン攻撃対策）。

## Issue 起票のルール

Issue の起票は **Linear MCP** を使用する。

- Linear MCP が未接続の場合は、起票内容のドラフトを用意した上で接続を促す
- プロジェクトごとのチームプレフィックスがある場合、PR 本文に `closes <PREFIX>-<番号>` を記載して紐付ける

## セッション運用

- 作業単位が変わったら `/clear` する。長い作業は着手前に計画を plans/ に書き出し、継続時は計画ファイルを再読して再開する（`/compact` は同一作業のまま容量が逼迫した時のみの次善手段）

## サブエージェント委任の基準

- 委任は「真に独立していて並列化できる大規模な作業」（例: 広範囲のマルチファイル調査）に限る
- 数回のツール呼び出しで自分で終えられる作業は委任しない。自分の作業の検証・ダブルチェック目的でサブエージェントを使わない
- 1体で足りるタスクに複数体を起動しない。生成数は常に最小限に抑える

## タスクのスコープと成果物

- 依頼されたスコープどおりに納品する。ルーティンの判断は自分で行い、解釈次第で成果物が大きく変わる場合のみ確認する。依頼に誤りやより良い代替があると考えた場合は1文で指摘し、黙ってスコープを狭めたり広げたり変形せず依頼どおり進める
- 書き出すドキュメント（レポート・ノート・ADR等）は必要な実質のみ書く。埋め草セクション・冗長な要約・定型文で水増ししない
